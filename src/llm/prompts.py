"""LLM提示词模板

定义所有LLM任务的提示词模板。
"""

# 中文摘要生成提示词
SUMMARY_PROMPT_CN = """请用简洁的中文（≤200字符）总结以下内容：

{description}

要求：
- 突出核心功能和价值主张
- 使用通俗易懂的语言
- 避免营销术语
- 严格控制在200字符以内

直接返回摘要，不要包含任何前缀或解释。"""

# 日文摘要生成提示词
SUMMARY_PROMPT_JA = """以下の内容を簡潔な日本語（≤200文字）で要約してください：

{description}

要件：
- 核心機能と価値提案を強調する
- 分かりやすい言葉を使う
- マーケティング用語を避ける
- 200文字以内に厳密に制限する

要約のみを直接返し、前置きや説明は含めないでください。"""

# 痛点提取提示词
PAIN_POINT_EXTRACTION_PROMPT = """分析以下文本，判断是否包含用户痛点（需求、问题、期望）：

文本：
{text}

上下文：
{context}

请返回以下JSON格式（如果不是痛点，返回空JSON）：
{{
    "is_pain_point": true/false,
    "keywords": ["关键词1", "关键词2", ...],
    "confidence_score": 0.0-1.0,
    "tags": ["标签1", "标签2", ...],
    "summary_cn": "中文摘要（≤200字符）",
    "summary_ja": "日文摘要（≤200字符）"
}}

痛点关键词示例：
- "need a tool for..."
- "wish there was..."
- "looking for..."
- "how to..."
- "problem with..."

直接返回JSON，不要包含任何其他文本。"""

# MVP建议生成提示词（简化版 - 只要产品概要）
MVP_SUGGESTION_PROMPT = """基于以下用户痛点和市场热点，生成一个创新的MVP产品概要：

用户痛点：
{pain_point}

市场热点：
{related_topics}

请返回以下JSON格式的产品概要：
{{
    "mvp_suggestion_cn": "中文产品概要（300字以内）",
    "mvp_suggestion_ja": "日文产品概要（300字以内）"
}}

产品概要必须包含以下三部分：

1. 核心功能（3-5个要点）
   - 这个产品具体做什么
   - 如何解决用户痛点
   - 关键特性和亮点

2. 目标用户群
   - 谁会使用这个产品
   - 用户画像（行业、角色、场景）

3. 变现方式建议
   - 推荐的收费模式（订阅制/一次性付费/免费增值）
   - 价格区间建议

示例格式：
\"核心功能：1) 自动从Reddit提取用户痛点并分类，2) AI分析变现潜力，3) 生成MVP建议报告。目标用户：独立开发者、创业者、产品经理。变现方式：订阅制，$29/月基础版，$99/月专业版。\"

要求：
- 不要包含技术栈、时间线、成本估算
- 聚焦产品价值和商业模式
- 简洁实用，易于理解
- 突出创新性和差异化

直接返回JSON，不要包含任何其他文本。"""
